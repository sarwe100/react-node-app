 steps:
  # running docker-compose
  - name: 'docker/compose:1.26.2'
    args: ['up', '-d']
    # [START cloudbuild_quickstart_build]

# [START cloudbuild_quickstart_build]
steps:
- name: 'gcr.io/cloud-builders/docker'
  args: [ 'build', '-t', 'us-west2-docker.pkg.dev/$PROJECT_ID/node-react-app-image:tag1', '.' ]
images:
- 'us-west2-docker.pkg.dev/$PROJECT_ID/node-react-apps-image:tag1'
# [END cloudbuild_quickstart_build]